/**
 * Add the name of this module to error messages to assist users in tracking
 * down issues.
 *
 * @param {string} message Error message text
 */
export default (message: string) =>
  `react-app-rewire-typescript-babel-preset: ${message}`;
